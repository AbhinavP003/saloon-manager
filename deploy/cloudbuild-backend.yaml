steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -t
      - ${_IMAGE}
      - .

images:
  - ${_IMAGE}

substitutions:
  _IMAGE: asia-south1-docker.pkg.dev/saloon-manager-beta-5640/saloon-repo/saloon-backend:gce
