steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --build-arg
      - NEXT_PUBLIC_API_URL=${_BACKEND_URL}
      - -t
      - ${_IMAGE}
      - ./frontend

images:
  - ${_IMAGE}

substitutions:
  _BACKEND_URL: http://34.29.107.247
  _IMAGE: asia-south1-docker.pkg.dev/saloon-manager-beta-5640/saloon-repo/saloon-frontend:gce
